[project]
name = "dj-midi-sender"
version = "0.1.0"
description = "XDJ-XZ Pro DJ Link beat broadcast → MIDI clock bridge"
requires-python = ">=3.10"
dependencies = [
    "mido>=1.3.0",
    "python-rtmidi>=1.5.0",
    "scapy>=2.5.0",
]
